function setbrand(item, brand) {
  var currentBrand = '';
  brand.forEach(function (current) {
    if (item.brand2 === current.id) {
      currentBrand = current.name;
    }
  })
  return currentBrand;
}
module.exports.setbrand = setbrand;